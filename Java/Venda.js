function Venda_1() {
  const preco = 5000;
  const taxaIva = 0.05;

  const valorIva = preco * taxaIva;
  const precoComIva = preco + valorIva;

  document.getElementById("resultado_1").textContent = precoComIva;
}

function Venda_2() {
  const preco = 3000;
  const taxaIva = 0.05;

  const valorIva = preco * taxaIva;
  const precoComIva = preco + valorIva;

  document.getElementById("resultado_2").textContent = precoComIva;
}

function Venda_3() {
  const preco = 6000;
  const taxaIva = 0.05;

  const valorIva = preco * taxaIva;
  const precoComIva = preco + valorIva;

  document.getElementById("resultado_3").textContent = precoComIva;
}

function Venda_4() {
  const preco = 6000;
  const taxaIva = 0.05;

  const valorIva = preco * taxaIva;
  const precoComIva = preco + valorIva;

  document.getElementById("resultado_4").textContent = precoComIva;
}

function Venda_5() {
  const preco = 7000;
  const taxaIva = 0.05;

  const valorIva = preco * taxaIva;
  const precoComIva = preco + valorIva;

  document.getElementById("resultado_5").textContent = precoComIva;
}

function Venda_6() {
  const preco = 4000;
  const taxaIva = 0.05;

  const valorIva = preco * taxaIva;
  const precoComIva = preco + valorIva;

  document.getElementById("resultado_6").textContent = precoComIva;
}